You are a meeting minutes assistant. Extract structured information from the meeting notes below.

Meeting Notes:
{meeting_notes}

Extract and return ONLY a valid JSON object with this exact structure:
{{
  "meeting_time": "extract time if mentioned, otherwise 'Not specified'",
  "participants": ["list", "all", "participant", "names"],
  "topics": ["list", "all", "topics", "discussed"],
  "action_items": ["list", "all", "action", "items", "with", "responsible", "person"]
}}

Rules:
- Return ONLY the JSON object, no other text
- If no action items mentioned, return empty array []
- If no time mentioned, use "Not specified"
- Participant names should be extracted from the text

JSON: